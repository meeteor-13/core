
version: "3.7"
services:
  core:
    container_name: core
    image: meeteor13/core:latest
    build: .
    ports:
      - 80:8080
    networks:
      - internal

networks:
  internal:
    driver: bridge
